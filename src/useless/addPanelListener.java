package useless;
/*package main_pack;

import java.awt.Color;
import java.awt.event.ActionEvent;
import java.awt.event.ActionListener;
import javax.swing.SwingUtilities;

public class addPanelListener {

	public addPanelListener(CenterView centerPanel) {

		centerPanel.addButton.addActionListener(new ActionListener() {
			public void actionPerformed(ActionEvent arg0) {

				SwingUtilities.invokeLater(new Runnable() {
					@Override
					public void run() {

						if (centerPanel.panelsList.size() < 6) {
							// SinglePanel tempPanel = new SinglePanel();
							tempPanel.setBackground(Color.black);
							centerPanel.panelsList.add(tempPanel);

							centerPanel.add(centerPanel.panelsList.get(centerPanel.panelsList.size() - 1));

							System.out.println(centerPanel.panelsList.size());

							centerPanel.frame.validate();
							centerPanel.frame.repaint();
						}
					}
				});
			}
		});

	}
}
*/